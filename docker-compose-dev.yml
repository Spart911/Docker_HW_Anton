services:
  web:
    image: nginx:latest
    deploy:
      placement:
        constraints:
          - node.role == worker
    ports:
      - "8081:80"

  db:
    image: postgres:latest
    deploy:
      placement:
        constraints:
          - node.role == worker
    environment:
      POSTGRES_PASSWORD: dev_password